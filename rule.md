# 摸鱼公司群机器人：全局规则

> 本文件是当前已实现玩法与经济系统的唯一规则基线。所有日期、跨日、定时任务与展示时间均以**北京时间（Asia/Shanghai）**为准。管理端的可配置值优先于下述默认值。

## 1. 基础身份与指令

- 玩家须先发送 `/入职 名字` 注册；同一平台账户只能拥有一名员工，重复入职不会覆盖原昵称或余额。
- 默认入职奖励为 `0` 摸鱼币，可在管理端配置。
- 当前基础查询指令：
  - `/我` 或 `/me`：余额、今日活跃等级、今日正向收益、连续打卡天数；
  - `/余额`：当前余额；
  - `/我的物品`：已拥有物品；
  - `/商店`：查看当前上架商品；
  - `/帮助`：查看启用的指令说明。
- 指令回复文案可在管理端编辑。模板变量由对应指令页面提供；编辑回复不会改变结算逻辑。

## 2. 经济总则

- 默认货币名称为“摸鱼币”，名称、入职奖励、打卡奖励与全勤奖均可配置。
- 余额允许为负数；惩罚、冻结与奖池扣除不会因余额不足而拒绝执行。
- 每一次余额变动都会记账，并带有来源（如打卡、活跃度、随机事件、小游戏、记忆考核）。
- `/我` 的“今日收益”只统计当天**正向**记账金额；扣款不会抵减该数字。
- “今日收益榜”同样仅按当天正向收益排名；只要当天有人有正向收益才会推送。默认推送时刻为 `12:00`、`16:00`、`20:00`、`23:59`，可在管理端调整。

### 2.1 随机运气红包

- 员工在群内发送 `/发红包 人数 总金额` 发出红包，其他员工（含发起者本人）发送 `/抢红包` 领取；私聊不会创建或领取，只会引导返回群聊。
- 人数只允许 `2–50`，总金额只允许“人数至 `99999`”的半角整数。同一时刻全群只允许一个未结束红包；红包独立于所有游戏和随机事件，不参与玩法互斥。
- 每名员工每天可成功发起 `5` 次；只有创建成功才消耗次数。创建时从发起者余额一次性扣除总金额，余额不足时拒绝创建。
- 红包默认 `10` 分钟过期，可配置为 `1–60` 分钟。每次领取立即公开领取金额与剩余份数；正金额实时入账，空包占一份但不产生余额流水。
- 每包最多出现一个空包。默认空包概率为 `5%`，可配置为 `0–30%`；两人总额为 `2`，或三人及以上且总金额小于人数两倍时，强制产生一个空包。无论是否有空包，最小值和最大值都必须各自唯一，中间金额允许相同。
- 全部领完后按领取顺序公布结果，并标出唯一的“手气最佳”和“手气最差”；空包存在时必为手气最差。过期时只退还尚未领取份额的金额，并公布已领取份数与退款金额，不评选最佳或最差。
- 重复投递同一平台消息不会重复创建、重复领取、重复扣款或重复入账。日期和每日次数均按北京时间自然日计算。

## 3. 打卡、连续打卡与全勤

- `/打卡` 每个自然日只能成功一次。
- 默认打卡奖励为 `5` 摸鱼币，可配置。
- 连续打卡天数以今天开始向前连续计算；当天尚未打卡时，从昨天开始计算连续天数。
- 全勤按自然周结算：每周一结算上一周（周一至周日）。上一周七天均有成功打卡，自动获得全勤奖。
- 默认全勤奖为 `5` 摸鱼币，可配置；同一员工、同一自然周只会结算一次。

## 4. 日活跃度与日收益

- 非指令的普通发言会计入当天累计发言字符数；空白与空格不计入，斜杠指令不计入。
- 活跃度对玩家只展示为 `LV0` 至 `LV10`，不直接展示累计字数。
- 默认等级门槛与次日自动入账奖励如下；十个等级的门槛、奖励均可配置：

  | 活跃等级 | 累计字符数门槛 | 奖励（摸鱼币） |
  | --- | ---: | ---: |
  | LV1 | 10 | 1 |
  | LV2 | 25 | 2 |
  | LV3 | 60 | 3 |
  | LV4 | 90 | 4 |
  | LV5 | 140 | 5 |
  | LV6 | 190 | 6 |
  | LV7 | 250 | 7 |
  | LV8 | 330 | 8 |
  | LV9 | 410 | 9 |
  | LV10 | 500 | 10 |

- 每天零点后的后台任务结算前一自然日活跃度奖励，同一员工、同一天只会结算一次。

## 5. 商店与物品

- 商店使用永久稳定的正整数商品编号。玩家在群内发送 `/商店` 查看本群商品，发送 `/购买 商品编号` 购买一件，发送 `/我的物品` 查看按编号聚合的个人库存；所有商店指令均禁止在私聊主动执行。
- 购买成功会在同一事务内扣除摸鱼币、减少有限库存、增加个人库存并记录流水。无限库存显示为“不限”；停用商品不能继续购买，但玩家已持有的系统卡仍可使用。管理员自建普通商品可购买和收藏，但没有系统效果，发送 `/使用` 时不会消耗。
- 初级、中级、高级、白金赠送卡分别售价 `3/5/10/20`，接收者到账 `2/4/8/15`，默认要求 LV2/LV4/LV6/LV9。四类赠送卡合计每天最多购买 `2` 张；使用时必须回复另一名已入职员工的消息发送 `/使用 商品编号`，不能赠送自己且不需要接收者授权。
- 刮刮卡 A/B/C 分别售价 `5/10/20`，随机获得 `1–10/5–15/10–30` 摸鱼币；C 卡默认要求 LV6。三类刮刮卡合计每天最多购买 `3` 张，玩家直接发送 `/使用 商品编号` 即时结算。
- 总监事对话卡售价 `2`，使用后增加当天 AI 对话额外额度 `1` 次；小游戏次数卡售价 `3`，使用后增加当天多人游戏发起额外额度 `1` 次。系统优先消耗职位或玩法原有额度，原额度用尽后才消耗卡片额度，额外额度跨日清零。

### 5.1 成人卡与知情授权

- 成人商品与普通商品共用商店，但只在后台为来源群开启“允许成人向商店玩法”后展示、购买和开始使用。关闭开关只阻止新流程，不中断已经进入填写、授权、生成或生效阶段的流程。
- M 卡直接发送 `/使用 商品编号`；机器人私聊依次收集 `1–200` 字场景和 `1–99` 的期望人数，然后回到来源群发布 `@所有人` 自愿参与公告。M 卡不创建报名队列，也不调用 AI。
- 其他成人卡须回复第一名目标发送 `/使用 商品编号`，再按私聊提示填写 `1–200` 字场景。3P、四人和六人卡还需分别回复其他目标发送 `/邀请参与 卡片局编号`，直到参与人数补齐。
- 每名目标会在来源群收到完整场景授权通知，并须在 `10` 分钟内回复该通知发送 `/同意使用` 或 `/拒绝使用`。同意表示确认本人已成年并同意本次具体场景；只有全部目标同意后才执行。任一拒绝或超时会让整局作废，预留卡不退回，系统向发起人补偿商品价格的 `50%`。
- 场景和选人阶段有效期为 `30` 分钟，可发送 `/取消使用 卡片局编号` 主动取消并退回预留卡；进入授权后不能取消。发起前必须先与总监事建立可送达的私聊房间。
- 授权完成后的场景由独立异步任务生成，最多 `800` 字，只回复到来源群最初的 `/使用` 消息，不阻塞普通消息，也不写入员工档案或稳定画像。后台可查看失败原因并重新生成。
- 常识改变卡分 `1/6/24` 小时三档，售价 `50/80/100`。全部授权后只在来源群为目标建立对应限时设定；同一员工同时只能有一条有效设定，到期自动公告恢复，后台可强制提前结束且不退款。

## 6. 随机事件（群内小剧场）

### 6.1 排期与场景

- 每天按固定时刻创建待开始场次。默认时刻为 `00:00`、`02:00`、`10:00`、`14:00`、`16:00`、`20:00`；场次数与时间均可在管理端配置。
- 固定的是推送时刻；每场使用的已启用场景与该场景内事件模板会随机选取并冻结到当日场次。
- 场景由以下内容组成：场景名称、报名公告、可报名身份及人数、事件名称、正式开场白、目标轮次与奖励。
- 报名公告按场景配置；正式开场白可使用身份变量（例如 `{主持}`、`{男1}`），实际发送时会替换为选择该身份的玩家昵称。
- 管理端可查看当日场次的“待开始 / 报名中 / 进行中 / 已结束”等状态，并可对待开始场次立即触发、调整时间、补充或移除今日场次。
- 同一群同时只允许一场随机事件处于报名中或进行中。若前一场跨天仍未结束，它会作为跨日进行中的场次保留；新的到点场次不会与它并行启动。

### 6.2 报名与进行

- 报名期默认持续 `15` 分钟，每 `5` 分钟提示一次剩余身份，二者均可配置。
- 玩家通过 `/加入 身份` 报名。身份容量已满、身份不存在、重复报名或事件已开始时不能加入。
- 所有席位满员后随机事件立即开始，机器人发送正式开场白。
- 进行中，参与玩家的普通发言会记录为事件详情并增加个人轮次；达到场景配置的目标轮次后，玩家可发送 `/退出` 领取该场景奖励。
- 未达到目标轮次而退出不能获得奖励；所有参与者都退出后，事件结束。
- 报名期退出会取消自己的报名；进行期退出后不能重新加入该场事件。
- 旁观者在事件报名中或进行中只能发送肉眼可见被中文全角括号 `（…）` 或英文半角括号 `(…)` 包住的内容；不符合格式会收到旁观警告。

### 6.3 随机事件优先级（硬性门禁）

- 报名中或进行中的随机事件拥有最高优先级，其他玩法和普通指令均不能打断它。
- 随机事件排期到点时，如单人或双人记忆考核、待选择的躲猫猫、谁是卧底任一玩法仍在进行，本次排期直接标记为已跳过，不打断当前玩法，也不在玩法结束后补触发。
- 管理员立即触发随机事件时，如上述任一玩法仍在进行，本次操作直接拒绝并提示“当前有游戏进行中”，排期继续保持待开始状态。
- 报名中默认放行 `/加入 身份` 与已报名玩家的 `/退出`；进行中默认放行参与玩家的普通剧情发言与 `/退出`。
- 管理员可分别配置“报名中允许执行的指令”和“进行中允许执行的指令”；现有任一群内指令均可被放行。被放行后会照常执行业务逻辑，例如放行 `/打卡` 后可正常打卡、入账与回复。
- 未被当前状态允许清单放行的指令或不合规发言均不会执行业务逻辑：不发奖励、不扣币、不创建玩法状态；机器人改为发送“随机事件拦截提示”。默认文案为“当前有随机事件发生，监事不会处理。”，管理员可在随机事件规则中编辑。
- 已加入玩家在进行中的正常剧情发言不属于指令拦截范围，仍会记录事件轮次；报名中或进行中，旁观者的合规括号发言同样不触发拦截提示。

## 7. 摸鱼躲猫猫（单人）

- 发起指令：`/开始摸鱼躲藏`；选择指令：`/躲 序号`。
- 系统从已启用的躲藏地点中随机展示 7 个候选地点（默认场景库含 10 个公司地点），玩家选择其中 1 个躲藏。
- 每人每天默认最多发起 `2` 次，可配置。若选择超时，默认 `2` 分钟后自动取消并返还该次次数，不扣币。
- 选择后系统进行两轮巡查：第一轮巡查 3 个地点；若未找到，再发送第二轮巡查 2 个剩余地点。每一轮分别发送消息。
- 默认规则为开局不扣费：被任一轮找到时扣除 `1` 摸鱼币；两轮都未找到时奖励 `3` 摸鱼币。惩罚金额、胜利奖励、每日次数、选择时限及场景库均可配置。
- 随机事件报名中或进行中时，不能发起躲猫猫；已存在待选择的躲猫猫时也不能重复发起。

## 8. 记忆考核

### 8.1 共通规则

- 记忆文本只由配置的字符集随机生成，默认包含大小写英文字母、数字和 `!@#$%&*_-`，不含空格和换行。
- 系统发送考题后按配置秒数撤回；**仅在机器人确认撤回成功后**才接受玩家作答。
- 玩家作答必须使用 `/答案 内容`；普通聊天和其他非玩法指令不参与答案判定，也不会计入答错。
- 同一时刻全群只允许一场记忆考核：单人挑战、对战等待中和对战进行中都占用该名额。
- 随机事件报名中或进行中时，不能发起或加入记忆考核。

### 8.2 单人挑战

- 发起：`/记忆考核`；默认每日限制 `1` 次，可配置。
- 默认共有 5 级：字符长度依次为 `5 / 7 / 9 / 11 / 13`，对应可收手奖励 `1 / 2 / 3 / 4 / 5` 摸鱼币；等级、长度、奖励和展示撤回秒数均可配置。
- 每级答对后，发送 `/继续` 挑战下一层，或发送 `/收手` 领取当前等级的对应奖励。
- 任一级答错会结束挑战，未收手的奖励全部失效；单人挑战没有额外失败扣币。
- 单人挑战不能跨自然日保留；新一天再次发起记忆考核时，前一日未结束的单人挑战自动过期，未收手奖励失效。
- 通过最高级会自动结算该级奖励。

### 8.3 双人对战

- 发起：`/记忆考核 对战`；另一位员工发送无参数的 `/加入` 入局。
- 发起后等待一名对手，不存在多个候选对局；满两人后才生成并展示考题。
- 默认使用第 5 级难度，双方各冻结 `5` 摸鱼币，基础奖池为 `10` 摸鱼币；难度、撤回秒数、基础奖池、错误冻结额、错误上限与作答时限均可配置。
- 撤回成功后，最先完整且准确发送 `/答案 内容` 的玩家赢得当前全部奖池。
- 每次答错，答错者默认再扣 `1` 摸鱼币加入奖池；达到默认 `10` 次答错即判定不合格。
- 一方达到答错上限只淘汰该玩家，另一位员工仍可继续作答；仅当双方都不合格时，系统才回收奖池。
- 玩家可发送 `/投降`；另一名仍合格玩家获得全部奖池。
- 若双方均不合格，或默认 `10` 分钟作答时限到期仍无人答对，系统回收奖池，不向任一玩家发奖。

## 9. 管理与数据一致性

- 所有经济配置、活跃度规则、收益榜时段、随机事件场景与排期、躲猫猫、记忆考核均由管理端维护；新增或修改配置只影响后续创建或结算，已开始玩法会使用创建时冻结的关键规则。
- 打卡、每日玩法次数、活跃度与全勤等自动结算均采用幂等记录；重复群消息或重复定时执行不应重复结算。
- 机器人使用独立浏览器用户目录保存平台登录状态；管理端人工登录使用三分钟操作租约，便于多人协作且支持其他管理员中断。

## 10. 职级、部门与晋升审批

- 职级与部门独立配置、独立存储。新员工默认职位为“实习生（LV1）”，默认部门为“未分配部门”。职位只代表群内玩法资格，绝不自动授予后台管理员权限。
- `/我` 或 `/me` 应展示昵称、职位和部门，以及既有的余额、今日活跃度、今日收益和连续打卡天数；`/职位` 展示所有启用职位及其晋升价格、按日均 90 摸鱼币估算的预计所需天数、投票权益、多人小游戏发起次数与群内管理资格。预计天数只作展示，不参与晋升结算。
- 部门预置：色色事业部、小游戏娱乐部、次元外联部、风纪监察部、核心技术部、摸鱼研究部、抽象艺术部、学院。`/部门` 仅展示启用部门及说明；新员工默认属于“未分配部门”。
- 部门可由管理端维护名称、说明和启用状态。“未分配部门”不可删除；已被员工使用的部门不可删除，停用部门不可再加入或切换。
- `/加入部门 部门名` 仅供未分配员工申请首次加入；`/切换部门 部门名` 仅供已有非默认部门的员工申请切换。普通员工的申请不即时变更部门：同一员工同一时刻最多一条待审部门申请，申请有效期为 24 小时，申请编号独立于晋升申请并递增不复用。核心董事会成员加入或切换任意启用部门时直接生效，无需审批。
- 普通申请仅由目标部门中、当前职位严格高于申请人的员工处理，申请人不能审批自己；目标部门无人或无更高职位成员时由核心董事会处理。核心董事会成员可跨部门查看、同意或拒绝全部待审部门申请；目标部门在申请后停用时，不得再同意转入。`/部门申请列表` 显示本人可处理申请；`/同意部门 1 2 3`、`/全部同意部门` 同意后才更新部门；`/拒绝部门 1 2 3`、`/全部拒绝部门` 和过期均不变更部门。所有处理动作记录审批人、时间和结果；批量按项独立处理。
- 晋升不是自动升级，也不按累计收入计算。员工发送 `/晋升` 只能申请下一档普通职位；申请时不扣币，晋升同意时才从当前余额扣除对应摸鱼币。余额不足时提示“你的摸鱼币不够呢”，申请保持待审。
- 每名员工同一时刻最多有一条待审晋升申请，申请有效期为 24 小时。申请会冻结当前职位、目标职位与价格；同意、拒绝或过期后关闭，关闭后可重新申请。
- 只有当前职位严格高于申请人当前职位的员工才可审批，且不能审批自己。`/晋升申请列表` 只显示本人有权处理的申请；申请编号全局递增且不复用。
- `/同意 1 2 3`、`/全部同意` 按项重新校验有效期、申请状态、审批资格与余额；一项失败不回滚其他有效项。`/拒绝 1 2 3`、`/全部拒绝` 按同一权限范围关闭申请但不扣币。全部审批动作都记录审批人、处理时间与结果。
- 默认职位及价格为：实习生 LV1（0）、正式员工 LV2（80）、小组长 LV3（200）、副主管 LV4（500）、主管 LV5（500）、部门副经理 LV6（800）、部门经理 LV7（800）、部门副总监 LV8（1000）、部门总监 LV9（1000）、公司负责人 LV10（2000）。LV8 及以上仅展示“群内管理资格”，具体群管理命令须后续单独定义。
- 核心董事会（LvMax）不可通过 `/晋升` 获得，只能由超级管理员授予或撤销；撤销时职位回落至公司负责人 LV10，不退款且不改变部门。核心董事会默认投票权益为 10、多人小游戏发起次数不限。

## 11. 谁是卧底（群内多人推理局）

### 11.1 开局与私聊发牌

- 同一群同一时刻只允许一场谁是卧底处于报名、发牌、发言、投票、并列补充发言或等待下一局状态。
- 玩家用 `/谁是卧底 人数` 创建报名局，人数只允许 `4–8`；报名期默认 `2` 分钟。其他玩家发送无参数 `/加入` 报名。
- 报名满员后，机器人按当局人数冻结角色配比并为每名玩家私聊发牌。默认配比为：4 人（平民 3 / 卧底 1 / 白板 0）、5 人（3 / 1 / 1）、6 人（4 / 1 / 1）、7 人（4 / 2 / 1）、8 人（5 / 2 / 1）。投票时长、白板胜利阈值和每档角色配比均可在管理端配置。
- 发牌仅允许发送到机器人已可访问的一对一私聊：玩家必须此前至少与机器人建立过私聊，使机器人可从自己的 `one_on_one` 房间中定位该玩家。机器人不创建私聊、不读取或持久化无关私聊内容。
- 所有玩家均确认收到私聊底牌后，群内才正式进入发言阶段；任一发牌失败则回退报名状态，不开始本局且不公开底牌、词语或私聊房间信息。

### 11.2 发言、投票与淘汰

- 在发言阶段，任一仍存活玩家可发送 `/开始投票`；每轮投票只允许投一次且不能修改，格式为 `/投票 序号`。
- 所有人投票完成时立即结算；投票倒计时结束但无人投票时回到发言阶段。投票最高且唯一的玩家被淘汰，群内仅公开其身份（平民 / 卧底 / 白板），绝不公开任一词语。
- 最高票并列时，并列玩家进入补充发言；补充后任一存活玩家再次 `/开始投票` 开启新一轮。
- 胜负按以下优先级判断：白板仍存活且总存活人数不高于白板阈值时白板胜利；没有卧底且没有白板时平民胜利；存活卧底人数不少于存活平民人数时卧底胜利。白板不计入卧底与平民人数比较。
- 对局存活玩家可发送 `/退出谁是卧底`，会公开其身份并触发胜负判断，退出后不能返回本局。报名期任一已报名玩家、或对局中的任一参与者均可发送 `/结束游戏` 立即释放本局，不结算胜者或奖励。

### 11.3 候场、续局与互斥

- 对局开始后，新玩家发送 `/加入` 会进入下一局候场队列，不会改变当前局人数或底牌。上一局结束后，任一原局未退出玩家或候场玩家发送 `/继续`，以“原局未退出玩家 + 候场玩家”为下一局候选，按 `4–8` 人重新配牌；人数不足 4 人不启动，超过 8 人按候场顺序截取。
- 结算后默认保留 `20` 分钟等待 `/继续`；超时后释放本局与候场队列。
- 谁是卧底、随机事件、记忆考核对战三者互斥：任一处于报名或进行状态时，其余两者都不能创建、加入或开始。单人记忆挑战不占用该互斥名额。
- 随机事件报名中或进行中时仍以第 6.3 节的指令放行清单为准：未被管理员显式放行的谁是卧底相关指令不会执行业务逻辑，而是发送配置的随机事件拦截提示。

## 12. 甩锅游戏

### 12.1 创建、报名与互斥

- 玩家发送 `/甩锅游戏 人数` 创建报名局；人数只允许 `2–10`。发起者自动加入，其他玩家通过无参数 `/加入` 报名。报名阶段按加入顺序展示临时编号；人满开局时按仍在场玩家的加入顺序重新连续编号为 `1–N`，此后编号冻结至本局结束。
- 创建者受现有职位“多人小游戏发起次数”限制，按北京时间自然日统计；仅报名局成功创建时消耗 `1` 次，创建后不因报名超时或参与者提前结束返还，`-1` 表示不限。加入者不消耗发起次数。
- 报名人数达到目标人数后立即开局。报名期默认 `2` 分钟，可在管理端配置；超时未满员则自动解散，不扣币、不发奖。
- 报名阶段，已报名玩家可发送 `/退出甩锅` 退出；目标人数保持不变，其他玩家继续等待。
- 游戏开始后，新的 `/加入` 不加入当前游戏，也不进入下一局候选队列；机器人提示玩家等待本局结束后重新创建或报名。
- 同一群同时只允许一场甩锅游戏处于报名或进行状态。甩锅游戏与随机事件、双人记忆考核、谁是卧底及其他占用群级多人玩法锁的游戏互斥；任一玩法占用该锁时，其余玩法不能创建、加入或开始。
- 本局任一参与者可发送 `/结束游戏` 强制终止报名局或进行中的游戏；非参与者不能终止。强制终止不判定胜负，进行中的保证金全部原路退回。

### 12.2 事故卡与开局

- 管理端维护甩锅事故卡。每张事故卡包含事故名称、事故描述、`1–4` 个不重复关键词和启用状态；支持新增、编辑、启用、停用和删除。
- 人满开局时，系统仅从已启用事故卡中随机抽取一张，并冻结事故名称、描述和全部关键词作为本局快照；对局开始后的事故卡修改不影响本局。
- 没有已启用事故卡时不能创建报名局。若报名期间所有事故卡均被停用或删除，人满时不启动游戏并解散报名局。
- 开局时，系统公开参与者固定编号、事故名称、事故描述和全部必用关键词，并在参与者中随机选择第一位持锅者。
- 甩锅游戏全程公开进行，不发送身份、线索或其他游戏私聊。

### 12.3 隐藏倒计时与温度

- 管理端按 `2–10` 人逐人数配置本局隐藏引爆时间的最短值和最长值。建议默认值如下：

  | 参与人数 | 隐藏引爆时间 |
  | ---: | ---: |
  | 2 | 45–75 秒 |
  | 3 | 60–90 秒 |
  | 4 | 75–120 秒 |
  | 5 | 90–135 秒 |
  | 6 | 90–150 秒 |
  | 7 | 105–165 秒 |
  | 8 | 120–180 秒 |
  | 9 | 135–210 秒 |
  | 10 | 150–240 秒 |

- 开局时，系统从对应人数的配置区间内随机生成本局引爆时长。具体秒数和截止时刻不向玩家公开。
- 全局引爆倒计时从正式开局开始持续运行，成功甩锅不会暂停、延长或重置倒计时。
- 每位持锅者另有一次操作时限，默认 `30` 秒，可在管理端配置。实际可操作时间取“本次操作时限”和“全局剩余时间”中的较小值；操作超时视为当前持锅者立即背锅，不再等待全局倒计时结束。
- 温度按本局总时长的剩余比例计算：剩余超过 `70%` 为“温热”；剩余超过 `40%` 且不超过 `70%` 为“发烫”；剩余超过 `15%` 且不超过 `40%` 为“滚烫”；剩余不超过 `15%` 为“即将爆炸”。
- 系统在开局、每次成功转交以及温度跨档时公布当前温度，但不展示剩余秒数。同一温度档的自动跨档提示最多发送一次；成功转交回复仍可附带当前温度。

### 12.4 甩锅指令与规则校验

- 仅当前持锅者可以发送 `/甩锅 玩家编号 甩锅理由`。群内普通消息不参与甩锅判定。
- 目标必须是本局另一名参与者，不能是自己。参与人数不少于 3 人时，不能立即甩回刚刚把锅交给自己的玩家；只有 2 人时允许双方连续互甩。经过其他玩家转手后，可以再次选择之前的玩家。
- 甩锅理由必须包含本局事故卡配置的全部关键词，每个关键词都须以完整连续文本出现。中文按原文匹配，英文字母忽略大小写，理由首尾空格不影响校验。
- 本局不允许重复使用完全相同的话术。比较前将英文字母转为小写、去除首尾空格、把连续空白合并为一个空格并删除 Unicode 标点类别中的全部字符；规范化结果与本局任一已经成功使用的话术完全一致时判定重复。调整语序、替换文字或使用近义词不属于完全重复。
- 关键词缺失、话术重复、编号不存在、选择自己、禁止回甩、非持锅者提交等情况均不转移锅，也不重置任何计时。持锅者可在自己的剩余操作时间内修改后重新提交。
- 关键词缺失时，机器人明确列出缺少的关键词；重复话术时提示玩家更换说法。话术只使用确定性规则校验，不调用 DeepSeek，也不判断内容是否合理或有趣。
- 有效甩锅请求在数据库事务内转移持锅者、记录规范化话术并设置新持锅者的操作截止时间，然后公开原持锅者、新持锅者和当前温度。

### 12.5 爆锅、主动退出与经济结算

- 甩锅游戏采用单轮制。全局倒计时归零或当前持锅者操作超时时，当前持锅者失败，其余参与者共同获胜，本局立即结束。
- 进行中的玩家可发送 `/退出甩锅` 主动退出；主动退出者立即视为失败者，按正常爆锅规则结算，整局同时结束。
- 一局有 `N` 名参与者时，失败者最终扣除 `N - 1` 摸鱼币，其余每名获胜者最终获得 `1` 摸鱼币；整局货币增减严格守恒。
- 为保证结算，人满开局时每位参与者暂扣 `N - 1` 摸鱼币作为保证金。正常结算时，失败者不退保证金；每名获胜者收到 `N` 摸鱼币，即退回自己的 `N - 1` 保证金并额外获得 `1` 摸鱼币。
- 玩家加入时必须至少拥有 `N - 1` 摸鱼币。人满准备开局时再次原子校验并暂扣所有人的保证金；此时余额不足的玩家会被移出报名名单，游戏不启动并继续等待其他玩家加入。
- 管理员强制终止、系统无法开始或其他不产生胜负的取消路径必须退回全部已扣保证金。扣保证金、退款、失败扣款结果、获胜奖励和对局结束必须采用幂等经济流水，重复消息或重复定时任务不能重复结算。

### 12.6 并发、超时与恢复

- 报名、开局、甩锅转移、主动退出和结算均在数据库事务内锁定当前游戏；同一时刻只有一个状态变更可以成功。
- 到期任务与甩锅请求并发时，以服务端接收时间为准：截止时间前收到且通过规则校验的请求可以转移锅，截止时间后收到的请求判定失败并进入结算。
- 两个甩锅请求并发时，只接受事务锁内第一个仍由当前持锅者发出的有效请求；后续请求按“非当前持锅者”处理。
- 报名状态、玩家编号、事故卡快照、全部截止时间、当前和上一位持锅者、成功话术及保证金状态均持久化。服务或 Worker 重启不会重置报名或游戏计时。
- 服务恢复时，未到期游戏按原绝对截止时间继续；已经超过全局或操作截止时间的游戏立即执行一次幂等结算。

### 12.7 管理端与回复模板

- 管理端新增“甩锅游戏”页面，提供总开关、报名超时、单次操作时限、`2–10` 人逐人数引爆时间区间、事故卡管理、当前对局状态和管理员强制结束功能。
- 温度比例采用第 12.3 节的固定规则，不按人数单独配置。每个人数的最短时间不得大于最长时间，所有秒数必须为正整数。
- 指令库提供可编辑回复模板，至少覆盖：创建报名、加入成功、报名退出、报名超时、开局、甩锅成功、关键词缺失、话术重复、目标无效、禁止回甩、非持锅者提交、四档温度、爆锅结算、主动退出、余额不足、事故卡不可用、玩法冲突、进行中拒绝加入和管理员终止。
- 管理端展示报名或进行中的对局状态，包括目标人数、当前玩家及编号、事故卡、当前持锅者、温度、报名或游戏状态；不展示隐藏的具体引爆秒数和截止时刻。

### 12.8 验收范围

- 覆盖 `2–10` 人创建、重复创建、报名、报名退出、人满自动开局、报名超时和进行中拒绝加入。
- 覆盖逐人数时间区间、隐藏秒数、四档温度、单次操作超时、全局引爆及成功甩锅不重置全局计时。
- 覆盖全部关键词、中文与英文匹配、完全重复话术、无效编号、选择自己、2 人允许回甩、3 人以上禁止立即回甩及失败后限时重试。
- 覆盖保证金原子扣除、余额不足移出、正常爆锅、主动退出、管理员终止退款、经济守恒和所有结算幂等性。
- 覆盖同一持锅者并发提交、提交与到期并发、服务重启恢复、过期后立即结算，以及与随机事件、双人记忆考核、谁是卧底等多人玩法的互斥。

## 13. 蹦蹦数字炸弹

### 13.1 创建、报名与开局

- 玩家直接发送 `/蹦蹦数字炸弹` 创建报名局，发起者自动加入；不再携带目标数量，也不设置参与上限。创建者及每位报名者必须先与总监事建立可定位的一对一私聊。
- 其他玩家发送通用指令 `/加入` 报名。报名期默认 `2` 分钟，可在管理端配置；到期仍未开始则自动取消并只通知一次。
- 至少有 `3` 名已报名玩家后，任一参与者可发送 `/开始` 开启第一轮。开局前系统再次核对全部玩家的私聊映射；缺失时保持报名状态并列出对应固定编号和名称。
- 游戏开始后，新的 `/加入` 进入下一轮候选；当前玩家发送 `/退出` 会在本轮结算后从下一轮退出。任一当前参与者可发送 `/结束游戏` 立即结束整场，管理员也可按精确对局强制结束。
- 玩家固定编号按首次报名顺序生成，退出或被跳过后不重排、不复用。

### 13.2 私聊报数、提醒与跳过

- 每轮开始时机器人先在群内公布轮次与参与者，再逐个私聊当前玩家：`请按这个格式报数给我 /报数 数字`。报数只能在对应私聊中发送，合法范围为 `1–100` 的整数；首份合法数字锁定且不可修改。
- 开轮后默认每隔 `15` 秒在群内通报尚未报数玩家的固定编号与名称，提醒间隔可配置。首次提醒成功后才开放 `/跳过`。
- 任一当前参与者可发送 `/跳过 2 4` 一次排除多个仍未报数玩家；`/跳过 唯一名称` 作为兼容写法，重名时必须改用编号。未知编号、重复目标、已报数玩家或已移除玩家均不会产生部分变更。
- 被跳过者立即从整场游戏永久移除，不进入后续轮次，不产生本轮胜负或玩家活动事实。若剩余玩家不足 `3` 名，整场立即结束；若剩余玩家均已报数，则立即结算本轮。
- 报数阶段和等待继续阶段都不因无操作自动解散。卡局只通过周期提醒、参与者 `/跳过`、参与者 `/结束游戏` 或管理员强制结束处理；服务或 Worker 重启后按数据库中的下一提醒时间继续，不重复提醒或结算。

### 13.3 计算、惩罚与续轮

- 收齐本轮有效数字后，系统从 `0.8、0.9、1.0、1.1、1.2` 中随机选择本轮倍率，精确计算 `F = 所有数字的平均值 × 本轮倍率`，并计算每个数字与 `F` 的绝对偏离；倍率在结算时公布。
- 最小偏离档位的全部玩家为胜出者；第二大偏离档位的全部玩家为受罚者。偏离值按从大到小公开排序；若不足三个不同偏离档位，本轮无效，保留同一轮次并清空数字，重新私聊当前玩家报数。
- 第 `1、2` 轮为真心话，第 `3` 轮为大冒险，此后三轮一循环。胜出者负责出题并监督，受罚者必须完成当轮对应惩罚。
- 有效轮结算后进入等待继续，任一参与者发送 `/继续` 开启下一轮；系统先应用本轮退出，再按候选顺序加入新玩家。应用后不足 `3` 名则结束整场。
- 只有有效结算才记录参与、胜出、受罚以及真心话或大冒险结果；指令、私聊数字、提醒过程、被跳过、报名超时、主动取消或管理员强制结束不进入稳定玩家印象。

### 13.4 管理与玩法引导

- 管理端可配置总开关、报名超时和未报数提醒秒数，并查看当前游戏、固定编号、已报数/未报数状态、报名截止和下一提醒时间；管理端绝不展示玩家提交的具体数字。
- AI 只解释上述规则并引导玩家发送 `/蹦蹦数字炸弹`、`/加入`、`/开始`、私聊 `/报数 数字`、`/跳过 编号`、`/退出`、`/继续` 或 `/结束游戏`，不会代替玩家执行命令或猜测私聊数字。

### 13.5 独立积分赛

- `/蹦蹦数字炸弹 积分赛` 创建独立的固定 `8` 人、`12` 轮积分赛；普通 `/蹦蹦数字炸弹` 行为不变。发起者自动报名，第 `8` 人加入后第一轮自动开始，不接受手动提前开局、超额报名或开局后的替补。
- 每轮继续使用随机倍率、私聊报数、每 `15` 秒提醒、真心话/大冒险循环和 `/跳过`。第 `1` 至 `11` 轮结算后由任一未退赛参与者发送 `/继续`，第 `12` 轮结算后自动结束并公布最终积分榜。
- 已报数玩家按绝对偏离从小到大排名：第 `1` 名 `+10` 分，第 `2` 名 `+5` 分，第 `3–6` 名 `0` 分，第 `7` 名 `-3` 分，第 `8` 名 `+2` 分。第一名并列时全部按第一名计分；最远偏离并列时全部按本轮实际报数人数对应的最后名次计分；其他并列取该并列组的起始名次，后续名次跳位。
- 积分赛中 `/跳过` 只处理当前轮：被跳过者本轮不参与排名并记 `-3` 分，下轮仍可参加，不从整场移除。未报数不会触发自动解散。
- 参与者发送 `/退出` 会立即退赛：若当前轮尚未报数，则当前轮记 `-3` 分；若已报数，则当前轮仍按报数正常排名；后续每轮自动记 `-3` 分且不再收到报数私聊。退赛者不可由新玩家替补。全员退赛时，系统自动补齐所有剩余轮次的退赛扣分并生成最终榜。
- 第 `1` 名负责出题并监督，第 `7` 名执行当前轮真心话或大冒险；并列时对应名次的所有玩家同时承担相同职责。报数人数不足时仅结算实际存在的名次，空缺名次不递补。
- 任一未退赛参与者可发送 `/结束游戏` 提前结束积分赛；正在收集但尚未完成的轮次整轮作废，不计任何积分，最终榜仅汇总已经完成的轮次。总积分相同允许并列。
- 管理端沿用现有开关、报名超时和提醒间隔配置；固定人数、总轮数和积分表不可编辑。当前游戏区域显示普通局/积分赛模式、当前轮次、每名玩家累计积分和退赛状态，但继续隐藏玩家私聊提交的数字。
- AI 需要同时解释普通局与积分赛，并引导玩家准确发送 `/蹦蹦数字炸弹 积分赛`、`/加入`、私聊 `/报数 数字`、`/跳过 编号`、`/退出`、`/继续` 或 `/结束游戏`。

## 14. AI 玩家印象

### 14.1 稳定印象与活动事实

- 系统可观察员工的普通群聊，并在积累足够有效消息后延迟形成稳定玩家印象；该后台更新不占用玩家的 AI 调用次数，也不阻塞群内回复。
- 斜杠指令、随机事件过程和小游戏过程不用于推断玩家印象。游戏与随机事件只在正式结算后记录参与、胜负等结果事实，不保存中间指令或过程话术。
- 管理员可在员工管理中查看、编辑、固定或删除稳定印象。管理员固定的印象不会被自动提炼覆盖或削弱。
- AI 回复可结合实时员工资料和稳定印象调整表达，但不能据此执行经济结算、裁决游戏或伪造系统状态；玩法操作仍须引导玩家使用准确指令。

### 14.2 AI 玩法引导

- AI 只负责解释玩法，并引导玩家自行发送当前已启用的准确指令；AI 回复本身不会调用指令处理器或改变任何业务状态。
- 余额、奖励、库存、开放部门、职位、游戏开关和当前对局等动态信息只使用实时业务数据，不从知识卡或玩家印象中猜测。
- 管理员可编辑稳定规则知识卡，但知识卡不能覆盖实时系统事实、准确指令清单或固定安全边界。
- 系统没有权威事实、启用指令或知识卡支持的业务问题，AI 不会猜测，会明确表示无法确认并引导玩家发送 `/帮助`。

## 15. 德州扑克（单局现金桌）

### 15.1 创建、报名与互斥

- 玩家发送 `/德州扑克 带入金额` 创建单局现金桌，发起者自动报名。带入金额默认只允许 `20–200` 的整数，其他玩家发送通用指令 `/加入`，并带入与发起者相同数量的真实摸鱼币。
- 发起者和加入者必须已经入职、余额充足，并已通过至少一条私聊消息让总监事获得可定位的一对一房间；不满足私聊条件时不能创建或加入。
- 每名员工按北京时间自然日最多成功发起 `1` 局；只有牌局实际开局才计数，报名取消、报名超时或董事会强制作废不计。参与他人牌局不受该次数限制。
- 牌局不预设目标人数，默认允许 `2–9` 人。至少 `2` 人后，任一参与者可发送 `/开始` 开局；报名等待默认 `120` 秒，到期未开局则自动取消并全额退款。
- 报名阶段任一玩家发送 `/退出` 只移除自己并全额退回带入金额；发起人没有额外结束权限。报名队列无人后立即关闭，否则继续等待原截止时间。
- 同一时间只允许一场德州扑克处于报名、发牌或进行状态，并与随机事件、记忆考核对战、谁是卧底、甩锅游戏、蹦蹦数字炸弹及其他群级多人玩法互斥。普通聊天与非游戏指令不受影响。

### 15.2 发牌、盲注与行动顺序

- 系统只负责安全随机洗牌、发牌、计时、裁判和结算，不作为玩家、不参与底池且不抽成。使用标准 `52` 张牌，无大小王；翻牌、转牌、河牌前各烧一张牌，同一局不会重复发出同一张牌。
- 牌面统一显示为 `♠️1`、`♥️J` 等格式，花色为 `♠️`、`♥️`、`♣️`、`♦️`，点数为 `1`、`2–10`、`J`、`Q`、`K`。`1` 仅是 A 的显示形式，牌型计算仍严格按标准德州扑克规则。
- 开局时随机座位和按钮位。三人及以上时，按钮位左侧依次为小盲和大盲，翻牌前由大盲左侧首位仍可行动玩家先行动，翻牌后由按钮位左侧首位仍可行动玩家先行动。两人单挑时按钮位同时是小盲，翻牌前先行动；大盲在翻牌后先行动。
- 小盲默认为带入金额的 `5%`、最低 `1`，大盲默认为 `10%`、最低 `2`，均向上取整为整数。带入范围、人数、盲注比例均可在后台配置，并在开局时保存快照。
- 系统向每名玩家的一对一私聊发送两张底牌。所有底牌消息确认送达后才进入首轮行动；发牌失败则暂停开局并提示对应玩家，不公开任何底牌。

### 15.3 下注指令与超时

- 德州扑克采用标准无限注规则，依次进行翻牌前、翻牌、转牌、河牌四轮下注。可用指令为 `/过牌`、`/跟注`、`/加注 本轮累计下注总额`、`/全下`、`/弃牌`；`/退出` 在开局后等同立即弃牌。
- `/过牌` 只在当前无需补齐下注时有效。`/跟注` 投入补齐当前最高下注所需的筹码；剩余筹码不足时自动以全部剩余筹码跟注并进入全下。`/全下` 投入全部剩余筹码。
- `/加注` 的金额表示该玩家在当前下注轮的累计投入总额。首次完整加注至少增加一个大盲，后续完整加注的增量不得低于上一笔完整加注增量；不足完整加注的短码全下不会重新开放已行动玩家的再次加注权。
- 只有当前行动者可以操作。每次操作后，系统以回复定位确认原指令，并公开操作结果、当前底池、玩家剩余筹码和下一位行动者。
- 每次行动默认限时 `120` 秒。截止时若可过牌则自动过牌，否则自动弃牌；超时不会重置或解散整局。计时使用持久化绝对截止时间，服务重启后继续原计时，已过期则立即执行一次幂等自动行动。
- 玩家可在私聊中发送 `/看牌` 重新获取自己的两张底牌；群内发送不会展示底牌，只提示转到私聊。系统不会接受玩家自行声称或展示的牌面作为裁判依据。

### 15.4 底池、摊牌与牌型

- 所有仍有行动权的玩家完成本轮、投入相同金额或进入全下后，系统才进入下一下注阶段。系统按每名玩家实际投入自动建立主池及任意数量的边池，并分别限制有资格争夺的玩家。
- 每名未弃牌玩家从自己的 `2` 张底牌与 `5` 张公共牌中任选 `5` 张组成最大牌型，不要求使用底牌。牌型顺序、同牌型比较、踢脚和并列判定严格遵循标准德州扑克。
- A 通常为最高点数；仅在 `A-2-3-4-5` 中作为最低点数，该组合是最小顺子。`10-J-Q-K-A` 是最大顺子，`Q-K-A-2-3` 不是顺子。
- 进入摊牌的玩家公开两张底牌、最佳五张成牌和牌型。弃牌玩家的底牌始终不公开；若其他玩家均已弃牌，最后一名未弃牌玩家直接赢得底池且不公开底牌。
- 并列最佳牌型的玩家平分其有资格争夺的底池；不能整除的 `1` 枚摸鱼币按按钮位之后的座位顺序分配。

### 15.5 真实摸鱼币结算

- 报名成功时从余额扣除完整带入金额，记录“德州扑克带入”流水，并将其转换为本局筹码。单局不允许补充筹码或二次买入。
- 报名退出、无人后关闭或报名超时取消时，逐人原额返还并记录“德州扑克退款”流水。
- 正常结束时，将每名玩家最终持有的全部筹码返还余额并记录“德州扑克结算”流水。全体返还金额必须严格等于全体带入总额，系统不增发、不回收且不抽成。
- 开局后发起人与普通参与者均不能发送 `/结束游戏` 终止整桌；`/退出` 只会弃牌，已经投入底池的筹码不退，未投入筹码在正常结算时按最终持有量返还。
- 仅核心董事会成员或管理后台可强制结束。强制结束按整局作废处理，不采用当前筹码与底池结果，而是逐人返还最初带入金额；强制作废局不消耗发起人的每日次数。
- 报名、行动、超时、底池分配和结算均在数据库事务内锁定牌局与相关余额。同一入站消息、定时任务或恢复任务重复执行不得重复下注、退款或结算；任一结算写入失败时整笔回滚，不允许部分到账。

### 15.6 管理、恢复与玩法引导

- 管理端提供总开关、默认 `2–9` 人、默认 `20–200` 带入范围、每日发起上限、报名超时、行动超时、小盲比例和大盲比例配置。配置变更只影响新开牌局，进行中的牌局继续使用快照。
- 管理端展示当前阶段、公共牌、底池、座位、已投入筹码、剩余筹码、玩家状态和当前行动者，并提供强制结束；不展示尚未公开的底牌。停用玩法只禁止新建，不自动结束当前牌局。
- `/当前游戏` 对参与者展示自己的座位、筹码、当前需跟金额和可用指令，对未参与者只展示公共状态。底牌只通过对应玩家的一对一私聊发送。
- `/帮助 德州扑克` 说明创建、加入、开始、看牌、下注、退出、结算和强制结束规则。AI 总监事只解释玩法并引导玩家发送准确指令，不代替玩家下注、不猜测底牌或改变游戏状态。
- 牌局、座位、牌堆、底牌、公共牌、下注轮、行动权、截止时间、底池资格、配置快照及经济状态全部持久化。Core、Browser Worker 或服务器重启后按数据库事实继续，不重新洗牌、发牌或计时。

## 16. 多群聊管理

### 16.1 全局数据与群级状态

- 系统支持由后台配置并同时监听多个群聊。员工身份、名称、工号、摸鱼币余额与流水、部门、职位、档案、形象、物品、商店、打卡、AI 额度、稳定画像和活动事实全局共享，不因群聊拆分或复制。
- 入站群消息、群回复、多人游戏、报名队列、随机事件、红包、躲猫猫等临时玩法、定时触发记录和群级 AI 对话上下文必须绑定来源群聊。
- 同一群同一时刻仍受原有玩法互斥规则约束；不同群可同时进行各自的游戏、随机事件和红包，互不占用对方的群级玩法锁。
- 同一员工允许同时参与不同群的玩法。涉及余额时继续锁定全局员工余额并原子结算，任何跨群并发都不得透支或重复发奖。
- 每日发起、参与和使用次数按员工全局共享；玩家在任一群消耗次数后，其他群同步计入，不能通过切换群绕过。核心董事会等既有不限次数权益保持不变。
- `/加入`、`/退出`、`/开始`、`/当前游戏`、`/结束游戏`、`/抢红包`及所有其他群级指令只查询并修改指令来源群的状态。
- 私聊命令只有一个候选群时沿用原格式；同一员工在多个群同时存在同类私聊交互时，机器人按群名称稳定列出本次候选序号，玩家须使用 `/报数 群序号 数字`、`/看牌 群序号` 等明确格式，系统不得猜测目标群。

### 16.2 后台群聊配置

- 后台“群聊管理”支持新增、编辑、停用和删除群聊。每项包含群聊名称、完整群聊链接、自动提取且唯一的 `chatroom_id`，以及启用监听、允许小游戏、允许随机事件、接收定时活动/公告、允许成人向商店玩法、开启公演预约、开启公司双色球等群级开关。
- 群聊链接只允许当前群聊平台同源的 `/chat` 链接并包含合法 `c` 参数；同一 `chatroom_id` 或规范化链接不能重复。后台不保存或展示登录 Cookie、Token 等凭据。
- 首次升级自动将 `DZMM_CHAT_URL` 对应的现有正式群写为“主群聊”，基础四个开关默认开启；以后数据库群聊配置为事实来源，环境变量只用于首次引导和故障兜底。新增的群级玩法开关默认开启，只有「允许成人向商店玩法」与「开启公演预约」默认关闭。
- 最后一个启用监听的群不能停用或删除。存在报名中、进行中或待结算玩法的群不能停用或删除，管理员须先按精确玩法强制结束。
- 停用后 Worker 退出该群并停止新收发，但历史数据保留。删除仅对已停用且无活动玩法的配置执行软删除，群聊记录继续保留以维持历史外键和审计可追溯性。
- 所有配置写入使用管理端版本号与 `If-Match` 防止并发覆盖，并记录操作者、修改前后内容和时间；群内董事会成员无权修改群聊配置。

### 16.3 多群监听与发送

- 单个 Browser Worker 动态加入全部启用群和已知一对一私聊房间。后台保存群聊后无需重启，Worker 在下一次配置同步周期加入新增群、退出停用群。
- 每条群入站消息必须携带来源 `chatroom_id`。Core 只接受已配置且启用监听的群消息；私聊继续按已知一对一房间独立识别。
- 普通指令回复、AI 回复、游戏通知、定时公告和长消息必须显式携带目标群 ID，不再依赖单一默认群。Bot Token 长消息也按消息对应目标群发送。
- 每个群使用独立发送顺序键；同一群内保持消息顺序，不同群可并发发送。消息去重键和历史补拉游标包含群 ID，避免不同群的平台消息编号相撞。
- Worker 断线恢复时重新加入全部启用群并分别补拉历史。单群连接失败不会阻断其他群，后台展示每群待连接、已连接、连接失败或已停用状态、最近收发时间和错误摘要。

### 16.4 群级功能开关

- “启用监听”关闭后停止该群全部新收发；关闭前必须没有活动玩法。
- “允许小游戏”关闭后阻止该群新建记忆考核对战、谁是卧底、甩锅、数字炸弹、德州扑克、红包、躲猫猫等玩法，但不打断已经开始的游戏，也不影响普通聊天、查询、档案和部门指令。
- “允许随机事件”控制该群是否按全局随机事件规则独立排期、报名和进行。关闭后不再生成新事件，已开始事件继续完成；投稿与审核仍为全局功能，已通过事件卡可供所有开启随机事件的群使用。
- “接收定时活动/公告”控制收入榜、考勤和玩法公告等全局定时消息是否发送到该群。同一份全局统计可发送到多个群，但经济奖励只结算一次。随机事件排期仅受“允许随机事件”控制。**公司双色球开奖公告是例外**：它属于公司级公共信息，无视本开关与彩票开关，广播到所有开着监听的群。
- “开启公司双色球”**只控制该群的彩票入口**：关闭后该群的彩票指令统一提示“本群未开放公司双色球”，但期次仍按全公司继续跑，开奖、福利与停售提醒照常广播，其他群不受影响。它不属于“允许小游戏”，彩票不占用「当前游戏」，也不与任何玩法互斥。

### 16.5 AI 与历史兼容

- 员工稳定画像可综合其在所有启用群的普通发言；AI 当前对话上下文只取来源群最近 `15` 轮，避免不同群的话题互相串入。
- 查询员工“最新情况”可使用该员工跨群最近 `30` 条有效发言，内部上下文须标注群聊名称与时间。斜杠指令、小游戏过程和随机事件过程继续不进入稳定画像。
- 管理后台查看员工消息时支持“全部群聊”与单群筛选。群聊停用或软删除后，历史消息仍用于审计和既有画像依据，但不再产生新消息。
- 数据迁移自动把现有群消息、群回复、活动玩法、红包、随机事件和定时触发记录关联到“主群聊”，并将原有全局活动唯一约束改为按群唯一，不删除或复制共享员工与经济数据。

## 17. 暗网交易所

### 17.1 上架与市场入口

- 已入职员工在私聊发送 `/上架暗网`，依次填写虚拟商品名称、用途、详细信息、匿名性别和起拍价。名称最多 `30` 字、用途最多 `100` 字、详情最多 `500` 字，性别只能选择男、女或保密，起拍价为 `1–99999` 的整数。
- 草稿有效期为 `30` 分钟。填写完成后先展示预览，玩家发送 `/确认` 才正式上架，发送 `/取消上架` 可取消未确认草稿；正式上架后卖家不能撤回。
- 暗网交易所全局共用一个市场，但后台必须指定一个已启用监听的“暗网群”。员工可在该群或私聊发送 `/查看暗网 [商品编号]` 查询竞价商品；同时兼容 `/登陆暗网`、`/登录暗网` 和 `/暗网` 三种别名。在暗网群发起查询时，结果只私聊发送给查询者，群内不展示商品内容；未建立私聊时须提示玩家先私聊总监事。其他群不能查询；新上架、报价、流拍、待收货、成交、投诉和身份公开也只向指定暗网群播报。
- 商品成功上架起独立计时，默认 `3` 小时后结算，后台可配置 `1–24` 小时。玩家查询结果不显示精确结算时间或倒计时。
- 每日上架次数按北京时间和职位全局计算：默认 LV1–2 为 `1` 次、LV3–4 为 `2` 次、LV5–6 为 `3` 次、LV7–8 为 `4` 次、LV9–10 为 `5` 次，核心董事会不限次数；后台可逐职位配置。

### 17.2 匿名报价与资金冻结

- 玩家在私聊发送 `/报价 商品编号 金额` 匿名报价。首次报价不得低于起拍价，后续报价至少比当前最高价高 `1` 摸鱼币；卖家允许给自己的商品报价。
- 报价成功立即冻结该玩家对应摸鱼币。同一玩家再次加价只补冻结差额；被其他玩家超过后，原最高报价的冻结金额立即全额退回。
- 群内只公布商品编号和新的最高报价，不公布报价人。每笔竞价、冻结、退回均须幂等并记录真实摸鱼币流水。

### 17.3 流拍、收货、投诉与身份公开

- 到期无有效报价则流拍，不收手续费。存在报价则进入 `72` 小时待收货期，最高报价继续托管；系统私聊买卖双方互相提供对方的“员工名称 + 工号”，方便线下核销，群内仍不公开两人身份。
- 买家在私聊发送 `/确认收货 [商品编号]` 后才正式成交；超过 `72` 小时未处理则自动确认。正式成交时才将托管款结算给卖家，并从卖家成交款中扣除手续费；默认费率 `5%`，后台可配置 `1%–100%`，手续费向上取整且至少 `1` 摸鱼币。
- 买家在待收货期私聊发送 `/投诉 [商品编号]` 后，订单进入“投诉审核中”，托管款继续冻结且停止 72 小时自动确认。暗网群只通报商品进入审核，不公开双方身份。
- 核心董事会后台管理员或超级管理员必须人工处理投诉，审核不设自动超时。投诉成立时系统全额退还买家托管款，并从卖家余额扣除与成交金额相同的罚款；卖家余额允许变为负数，不收手续费、不支付成交款，暗网群公开卖家名称与工号并通报批评，买家保持匿名。驳回投诉时按确认收货结算，支付卖家成交款并扣除手续费，再进入双方身份公开确认。
- 卖家自购同样进入待收货流程并正常收取手续费；系统只发送一条私聊通知。确认、投诉提交、审核、退款、罚款和结算必须在同一数据库事务中完成，任何一步失败都不得产生部分到账或重复资金变化。
- 确认收货或自动确认后，系统私聊买卖双方进入 `10` 分钟身份公开确认。只有双方都发送 `/公开 [商品编号]` 才在暗网群同时公开两人身份；任一方发送 `/不公开 [商品编号]` 或超时未回复，则双方都保持匿名。投诉订单不进入公开确认；自购成交只需该玩家确认一次。
- 同一买家只有一笔待收货交易时，`/确认收货` 与 `/投诉` 可省略商品编号；存在多笔时系统必须列出候选编号，不得猜测目标交易。公开身份确认沿用同样的可选编号规则。

### 17.4 管理与持续运行

- 后台可配置总开关、暗网群、竞价时长、手续费和职位每日次数，并查看全部商品、报价记录、真实买卖双方与工号、待收货期限、处理时间及经济流水。
- 管理员可强制下架竞价中的商品；系统立即退回当前最高报价的全部冻结金额，不收费，并在暗网群公告。普通玩家不能撤回已上架商品。
- 关闭总开关只禁止新建和确认上架草稿，不阻断现有商品继续报价、到期结算、收货处理或身份确认。商品、报价、冻结、收货截止时间、处理结果和公开选择全部持久化，Core、Worker 或服务器重启后继续处理。
- `/帮助 暗网交易所` 与 AI 总监事需要解释准确指令、匿名规则和资金规则；AI 只做说明和指引，不代替玩家上架、报价或确认公开身份。

## 18. 公演场次预约

### 18.1 预约、档期与审核

- 公演是独立于小游戏和随机事件的群级子系统。后台须先为承办群开启“公演预约”；员工和经济数据继续全局共享。
- 任一已入职员工可在群内发送 `/预约公演`，随后按私聊向导依次填写标题、简介、开始时间、参演人员和可选封面。标题 `1–50` 字、简介 `1–500` 字、参演人员 `1–30` 名且必须为名称唯一的已入职员工；封面仅支持不超过 `10MB` 的 JPEG、PNG 或 WebP，发送 `/跳过` 可不设置封面。
- 时间格式固定为 `YYYY/MM/DD-HH:MM:SS`，按北京时间解析，须至少提前 `30` 分钟且不超过未来 `30` 天。草稿 `30` 分钟失效，发送 `/确认` 前不占用档期。
- 全部群共用一张公演日历，每个北京时间自然日最多一场待审核或未结束公演；同一员工同时只能发起一场未结束预约。待审核预约也占用当天档期。
- 提交后由后台审核。审核通过或拒绝会私聊发起人；若到开始前 `5` 分钟仍未完成审核，预约自动过期并释放档期。
- 私聊 `/我的公演预约` 查看自己的预约和延期状态，群内 `/公演日程` 查看本群未来已审核场次。发起人可在预告前私聊 `/取消公演预约`；预告发布后须联系董事会成员在后台处理。

### 18.2 延期

- 发起人可私聊发送 `/延期 30m`、`/延期 1h` 或 `/延期 1d`；单位只接受小写 `m/h/d`，时长必须为正整数。每场同时只能有一条待审核延期。
- 延期先申请、后生效。批准时会重新检查未来 `30` 分钟至 `30` 天范围以及目标日期占用，并在一个事务中同时释放旧日期、占用新日期；失败时原档期保持不变。
- 预告前普通管理员和董事会成员均可审批；预告后仅董事会成员可审批。预告后获批会立即发布时间变更通知，并在新开始时间前重新预告。
- 延期申请不会暂停原定开场；到原定开始时间仍未审批即自动失效。进入等待开场、演出、打赏或终态后不能延期。

### 18.3 预告、演出与打赏

- 系统在开场前 `5` 分钟发布固定预告。预告后，本群禁止新建小游戏或触发随机事件；其他群不受影响。若同群已有玩法，系统不打断，公演进入等待状态并在前序玩法结束后自动开场。
- 到点先发送固定开场文本，再单独发送封面；没有封面则只发文字。公演最长时长默认 `360` 分钟，可在后台配置，超时自动结束演出。
- 演出中，参演人员普通发言视为台词且机器人不回复；其他员工只能用完整 `()` 或 `（）` 进行场外交流。AI、普通指令、系统通告、游戏和随机事件均不能插入。任一参演人员可发送 `/end` 结束演出部分。
- 演出结束后固定开放 `180` 秒打赏。仅允许 `/打赏 参演人员名称 金额`，或回复该参演人员本场台词发送 `/打赏 金额`；普通聊天仍须使用完整括号。
- 打赏金额须为正整数，只能给本场参演人员，不能给自己，余额不足时拒绝。摸鱼币实时从打赏人转入参演人员，不收手续费，并按入站消息幂等。
- 打赏结束后公布总额、每名参演人员所得和金额最高的前 `5` 笔明细，再发送固定演出结束线。每名参演人员只记录一条“参与了《公演标题》公演”的活动事实；台词、场外聊天和打赏指令不进入 AI 对话记忆。
- 演出和打赏期间，同群系统公告按原顺序暂存，最终结算消息之后再释放；私聊中的退款、扣款、通知及其他群消息不延迟。

## 19. 记忆考核公会赛

### 19.1 创建与队伍

- 任一已入职员工可在群内发送 `/记忆考核 公会赛 场数` 创建比赛并担任主持人；场数为 `1–20`，不限制每日发起次数。主持人不能加入队伍。
- 主持人用 `/队伍1 队名`、`/队伍2 队名` 设置队名，用 `/队伍1人员 名称列表`、`/队伍2人员 名称列表` 设置名单。成员必须已入职、名称唯一，且不能同时属于两队。
- 第一场进入选人后，队名和人员名单锁定。正式场次每名选手整场只能上场一次，因此两队人数都必须不少于预设正式场数。
- 公会赛按群隔离并占用来源群的多人玩法锁；不同群可同时比赛。同群随机事件按现有冲突规则跳过，不打断比赛。

### 19.2 场次、选人和答题

- 主持人逐场发送 `/第N场 胜场数/总局数`，例如 `/第1场 4/7`。场次必须连续，赛制必须为严格过半的奇数局制。
- 每场由两队成员分别私聊发送 `/上场 玩家名称`。每队首个有效选择立即锁定，双方都提交后才在群内同时公布；多个群有候选时使用 `/上场 群序号 玩家名称`。
- 每个小局均由主持人发送 `/开始对战`。系统固定使用记忆考核第 `5` 级题目，复用现有对战展示秒数、撤回机制和作答超时。
- 当前两名选手在群内发送 `/答案 内容`；答错可继续提交，第一个完全答对者赢得该小局。超时无人答对则为平局，不计胜场，主持人重新发送 `/开始对战` 开新题。
- 某队率先达到本场目标胜场后赢得该场。即使一队已提前锁定大比分优势，也必须打完全部预设场次。

### 19.3 加赛、结束与后台

- 全部预设场次结束后，大比分较高的队伍夺冠；若大比分相同，主持人按顺延编号创建加赛。加赛允许复用已经出场的选手，加赛胜方直接成为冠军。
- 公会赛不收取或奖励摸鱼币，不支持投降，也没有等待操作超时。仅主持人可发送 `/结束游戏`；董事会成员和管理后台可强制结束。
- 题目、答案和过程指令不进入长期记忆。正常结束后仅为参与者记录所属队伍与胜负事实，为主持人记录主持事实；强制结束只记录参与。
- 后台显示当前比赛、两队名单、当前对阵与比分，并可强制结束；历史分页保存队伍比分、各场对阵、每小局胜负或平局、作答记录和最终冠军。
- 比赛阶段、选人、题目、截止时间和全部结果持久化；Core、Worker 或服务器重启后按数据库状态继续，超时任务重复执行不得重复结算。

## 20. 公司双色球（号码型彩票）

### 20.1 号码、奖金与投注

- 公司双色球是独立于小游戏和随机事件的号码型彩票，不接入玩法 gate，不占用「当前游戏」，也不与任何玩法互斥。**期次编号、号码、奖池、调节金与开奖时刻全公司唯一**：三个群买的是同一期、看的是同一组号码、同一时刻开奖，期号全局递增；员工、余额与流水本来就全局共享。后台关闭总开关只阻止新的购票与草稿，不中断已经开卖期次的停售、开奖与派奖。
- 红球从 `01–10` 中选 `4` 个不重复号码，蓝球从 `01–06` 中选 `1` 个，共 `C(10,4) × 6 = 1260` 种组合；号码池、红球个数与蓝球池均可在后台配置。
- 奖级、命中条件、概率与默认奖金如下，五个奖级的奖金均可配置：

  | 奖级 | 命中条件 | 概率 | 默认奖金 |
  | --- | --- | ---: | ---: |
  | 一等奖 | 4 红 + 蓝 | 1/1260 | 100 |
  | 二等奖 | 4 红 | 1/252 | 50 |
  | 三等奖 | 3 红 + 蓝 | 1/52.5 | 15 |
  | 四等奖 | 3 红 | 1/10.5 | 5 |
  | 五等奖 | 蓝球中且红球中 0–2 个 | 1/6.81 | 1 |

- 任意中奖概率为 `26.59%`，每注长期期望返回约 `1.19` 摸鱼币，返还率约 `59%`；对玩家而言买票整体是净支出。玩法本身不销毁货币：每注差额在结算时经奖池溢出转入调节金，再以全员福利发回员工；除全局一次的启动奖池与后台手动注资外不额外增发货币。
- 彩票的余额流水**不计入「今日收益」与「今日收益榜」**：中奖与全员福利是零和再分配而非劳动收益，购票支出也不抵扣，否则一次头奖就能把人顶上收益榜。玩家自己的彩票盈亏看 `/我的彩票`（累计投入／累计中奖／净收益），全公司口径看后台彩票报表的对账。流水来源使用稳定 token（`company_lottery_purchase`、`company_lottery_prize`、`company_lottery_welfare`）。
- 单注默认 `2` 摸鱼币；每人每个北京时间自然日默认最多买 `5` 注，单期默认最多售出 `2000` 注，均可在后台配置。同一期同一员工的同一组号码只能买一次。
- 三种购票方式：
  - `/购买彩票 03 07 09 10 + 05`：手选一注。`+` 可省略，号码可用空格、英文逗号或中文逗号分隔；号码越界、红球重复、红球个数不符会分别指出具体原因。
  - `/购买彩票 机选 [注数]`：随机一注或数注。同一次机选生成的多注互不重复，并自动避让本人本期已购号码；别名 `随机`、`机`、`random`、`quick`。
  - `/购买彩票 N 注`：进入逐注填写。逐条发送号码或「机选」可在同一草稿内混用；发送 `/确认彩票` 才一次性扣款并按注落库，发送 `/取消彩票` 放弃且不扣款。
- 逐注填写的目标注数按当日剩余额度裁剪；号码格式对得上但草稿已超时会明确提示重新发起，不会被当成闲聊静默丢弃。草稿默认 `15` 分钟超时，超时作废且不扣款；草稿内重复号码会被拒绝。
- 手选与机选共用每日 `5` 注限额，并走同一套号码生成与判档逻辑，注单只额外记录是否为机选。
- 指令别名 `/买彩票` 等价于 `/购买彩票`；全部彩票指令均为群内指令，私聊只提示回群。`/彩票` 展示本期期号、票价、已售与本人注数、停售与开奖时刻、奖池与调节金、承诺哈希前 `16` 位、号码池与各奖级概率；`/我的彩票` 展示近期注单与累计投入、累计中奖、净收益。

### 20.2 开奖、奖池与结算

- 每期开卖时生成红球与蓝球并写入期次，同时公布 `SHA256(号码串|盐)` 前 `16` 位作为承诺；号码与盐在停售前不对外返回。已开奖期次可用 `/彩票验证 期号` 查看号码、盐与完整承诺哈希，玩家可自行核对。
- 默认每晚 `22:00` 开奖，提前 `10` 分钟即 `21:50` 停售，停售前 `5` 分钟发一次提醒；开奖时刻、停售提前分钟与提醒提前分钟均可配置，全部按北京时间计算。提醒只在该群最近一条入站消息距今超过 `30` 分钟时发送，且**每期最多发一次**，避免刷屏。
- 停售后拒绝一切购票。到点后系统公布号码、按员工合并后的中奖明细、实付金额、奖池与调节金余额以及单人单期中奖上限，并自动开出下一期。
- 当期全部购票金额先进入奖池。结算时若奖池超过上限（默认 `200` 摸鱼币），超出部分转入调节金且不再滚回奖池；一等奖无人中时奖池自然滚存到下一期。
- 结算顺序固定：投注额入池 → 奖池封顶溢出 → 逐票判档 → **同一员工多张中奖票先合并** → 合并后按员工封顶（默认 `100` 摸鱼币）→ 应付超过可付款时按比例折算 → 发放。折算发生在封顶之后并向下取整，尾差补给该员工最后一张中奖票，不会凭空吞币。
- 任何情况下不超发、不出现负余额、不允许系统凭空补币。
- 每一期的结算都要过守恒校验：`期初 + 售票 = 派奖 + 溢出 + 期末`，且员工侧实发合计等于实付；奖池账本写完后还要与期末余额复核，任何一处对不上直接报错回滚。调节金同样校验 `发放前 = 发放额 + 剩余`。开奖公告里固定带出这一行对账信息。
- 系统在首期注入 `100` 摸鱼币作为启动奖池，之后不再额外注入，金额可配置。**全公司只注入一次**，不按群重复注入。后台手动注资与它一起计入「系统注入」。

### 20.3 调节金与全员福利

- 每期开奖结算后检查调节金余额：达到「当前可发放员工人数 × 每人金额」时触发全员福利。默认门槛即员工总数、每人 `1` 摸鱼币；开关、每人金额与入职时长门槛均可配置。
- 每次只发一轮，不是能发几轮发几轮；余数保留，参与下次触发。门槛始终取最新人数，员工数变化后下一次触发即按新人数计算。
- 纯按人头发放，不开放活跃度、不按投入比例；员工数为 `0` 或每人金额为 `0` 时不触发。
- 读取余额、判定门槛、全员加币、写发放明细、扣调节金在同一个事务内完成，中途失败整体回滚，下次开奖后重试。
- 调节金与奖池各走一本账本、逐笔留痕，任意时点可对账；两本账都是全公司共用，不按群拆分。

### 20.4 管理与持续运行

- 后台可配置总开关、号码池与选球数、单注价格、每人每日注数上限、单期注数上限、奖池上限与启动奖池、单人单期中奖上限、五个奖级奖金、开奖与停售时刻、停售提醒分钟、草稿超时，以及全员福利开关、金额与入职时长门槛。
- 开奖公告是**公司级公共信息**，广播到所有已配置的群：群级彩票开关与「接收定时活动/公告」都不影响它，关掉彩票入口的群照样能看到今天开出了什么号。唯一例外是关掉「启用监听」的群——那等于该群停止全部新收发，不再投递。
- 彩票不接入玩法 gate，但每个群有独立的「开启公司双色球」开关，**只控制该群的彩票入口**：关掉之后该群不能用任何彩票指令（统一提示回开放了的群），但期次仍按全公司继续跑，开奖公告照发，其他群不受影响。
- 后台可查看全公司的期次记录（状态、售票、流水、期初/溢出/期末奖池、中奖人数、应发与实发、开奖号码与盐）、奖池与调节金流水、中奖明细，以及每名员工的累计投入、累计中奖与净收益；并支持手动开奖与手动注资。
- 后台固定展示对账结果：`系统注入 + 售票 − 员工侧入账 = 奖池 + 调节金`，不符时明确标红。系统注入包含启动奖池与后台手动注资。
- 手动开奖只对已过停售时刻的期次生效，且走与定时任务完全相同的流程（停售 → 开奖 → 公告 → 全员福利），保证不会在号码承诺期内提前泄露号码；手动注资可分别注入奖池或调节金。
- 购票、扣款、入池、开奖、派奖、停售提醒与福利发放全部按入站消息和期次幂等：重复的平台消息不会重复扣款，重复的定时任务不会重复开奖、重复派奖、重复发福利或重复提醒。期次、注单、草稿、账本、福利记录与提醒占位全部持久化，Core、Worker 或服务器重启后按数据库事实继续。
- `/帮助 彩票` 与 AI 知识卡说明购票指令、奖级概率、开奖时刻与全员福利规则，并如实写明每注长期期望返回约 `1.19` 摸鱼币。彩票不写入长期记忆，也不产生玩家画像事实。
